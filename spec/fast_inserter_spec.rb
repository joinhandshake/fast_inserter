require 'spec_helper'

describe FastInserter do
  it 'has a version number' do
    expect(FastInserter::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
